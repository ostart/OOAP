// Отличные примеры динамического связывания, что на Java, что на Python. 
// Единственное, что смутило, базовый класс на Python называется IHasher. У нас в C# так обычно обозначаются интерфейсы, что вызывает когнитивный диссонанс.
// Но т.к. класс IHasher содержит абстрактный метод, то по смыслу (семантике) он соответствует интерфейсу. 
// Возможно в Python нет (и не треубется) интерфейсов как самостоятельных конструкций языка